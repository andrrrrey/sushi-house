# Журнал разработки

## Этап 0 — доступ и исходные условия

Статус: завершён.

Зафиксировано 11 сентября 2026 года:

- сервер: `5.129.249.100`;
- выделенный сервер с административным SSH-доступом;
- домен будет подключён позже;
- до появления HTTPS админка будет доступна только через SSH-туннель;
- временная пара ключей Ed25519 создана локально;
- отпечаток публичного ключа: `SHA256:4+RVbH3vvRNokKr45Ym+0QrvUtD69igH73EXfXpi7+Q`;
- закрытый ключ находится в исключённой из Git папке `.secrets/`;
- адрес GitHub-репозитория ожидается;
- SSH-подключение: `root@5.129.249.100:22`.

Результат аудита:

- ОС: Ubuntu 24.04.4 LTS, ядро Linux 6.8;
- ресурсы: 2 CPU, 3.8 ГБ RAM, 48 ГБ диска, свободно 46 ГБ;
- swap отсутствует;
- Docker, reverse proxy и Asterisk не установлены;
- активные пользовательские сервисы отсутствуют, кроме Zabbix Agent;
- слушают SSH `22/tcp` и Zabbix `10050/tcp`;
- UFW не активирован;
- выбран способ развёртывания: приложение и PostgreSQL в Docker Compose, админка до подключения TLS доступна только на `127.0.0.1` через SSH-туннель.

Критерии завершения этапа:

- публичный ключ установлен на сервер;
- выполнен успешный вход по SSH;
- проведён аудит ОС, ресурсов, сети и уже запущенных сервисов;
- определён безопасный способ развёртывания без нарушения существующей конфигурации.

## Последующие этапы

1. Серверный каркас и база данных.
2. Админка, авторизация и защищённые настройки.
3. iikoCloud в режиме чтения.
4. Mango SIP и тестовые звонки.
5. HTTPS callbacks и приёмочные проверки.
6. Подключение OpenAI Realtime и сценариев робота после отдельного согласования.

## Этап 1 — серверный каркас

Статус: завершён 11 сентября 2026 года.

Выполнено:

- установлен Docker Engine 29.1.3 и Docker Compose 2.40.3 из репозитория Ubuntu;
- проект развёрнут в `/opt/sushi-house-voice-robot`;
- создан отдельный внутренний пароль PostgreSQL без вывода в терминал и логи;
- приложение FastAPI и PostgreSQL запущены через Docker Compose;
- оба контейнера имеют health-check и находятся в состоянии `healthy`;
- `/health` возвращает `{"status":"ok"}`;
- `/ready` подтверждает подключение PostgreSQL;
- админка привязана только к `127.0.0.1:8080`;
- порт PostgreSQL не опубликован на хост;
- `.dockerignore` и `.gitignore` исключают секреты, записи и runtime-данные.

Проверка доступа с рабочего компьютера:

```bash
ssh -L 8080:127.0.0.1:8080 root@5.129.249.100
```

После установки туннеля стартовая страница доступна по `http://127.0.0.1:8080`.

## Этап 2 — админка и защищённые настройки

Статус: завершён 11 сентября 2026 года.

Выполнено 11 сентября 2026 года:

- дизайн переработан по предоставленному референсу: glassmorphism, тёплый фон, молочные акценты, компактная навигация;
- добавлена адаптивная вёрстка для настольных и мобильных экранов;
- добавлена первичная регистрация единственного администратора;
- после создания администратора endpoint первичной регистрации автоматически закрывается;
- пароли хешируются Argon2;
- сессии подписываются отдельным случайным серверным ключом;
- изменяющие формы защищены CSRF-токеном;
- добавлены защитные HTTP-заголовки и запрет кэширования закрытых страниц;
- создано зашифрованное хранилище настроек на Fernet;
- чувствительные значения после сохранения не выводятся обратно;
- добавлен аудит создания администратора, входов и изменения настроек;
- в каталог включены параметры iikoCloud, Mango SIP, Mango API и будущий OpenAI API Key;
- приложение версии `0.2.0` развёрнуто и прошло проверку health/readiness;
- визуально проверена страница первого запуска через SSH-туннель.
- владельцем создана первая учётная запись администратора, повторная первичная регистрация закрыта;
- GitHub deploy key подключён с правом записи;
- исходный код этапов 1–2 опубликован в ветке `main` репозитория `andrrrrey/sushi-house`.

## Этап 3 — iikoCloud в режиме чтения

Статус: завершён 11 сентября 2026 года.

Выполнено:

- отдельный раздел диагностики iikoCloud;
- авторизация приложения через `/api/v2/access_token`;
- получение организаций, терминальных групп, списка внешних меню и стоп-листов;
- получение доставочных заказов за выбранный период до семи дней;
- явное различение успешного ответа API и фактического количества найденных заказов;
- вывод номера, организации, статуса, источника, клиента, курьера и суммы без операций изменения;
- модульные тесты клиента на подменённом HTTP-транспорте;
- версия `0.3.0` развёрнута на сервере, контейнеры прошли health-check;
- ранее предоставленные iiko-доступы переданы по SSH и сохранены в зашифрованном виде;
- реальная проверка API вернула 10 организаций, 11 терминальных групп, 1 внешнее меню и 8 групп стоп-листов;
- запрос заказов за 10–11 сентября 2026 года завершился без ошибок API, но вернул 0 заказов;
- успешный HTTP-ответ не трактуется интерфейсом как наличие заказов.

Следующий этап: Mango SIP в изолированном тестовом режиме, без направления рабочего номера на робота.

## Промежуточное обновление — публичный HTTPS-доступ

Статус: завершено 11 сентября 2026 года.

- установлен Nginx как reverse proxy к приложению на `127.0.0.1:8080`;
- выпущен TLS-сертификат Let's Encrypt с автоматическим продлением;
- временный адрес: `https://5-129-249-100.sslip.io`;
- HTTP перенаправляется на HTTPS;
- cookies сессии переведены в режим `Secure`;
- добавлено ограничение частоты обращений к форме входа;
- добавлены HSTS и Content Security Policy;
- прямой порт приложения по-прежнему доступен только локально на сервере;
- приложение принимает только утверждённые значения заголовка Host.

## Исправление iiko — назначенный курьер

Статус: завершено 14 сентября 2026 года.

- подтверждено получение заказов после настройки интегратора;
- поле курьера уточнено по фактическому ответу API: `courierInfo.courier.name`;
- исправлён парсер вложенной структуры;
- телефон курьера не выводится в таблице админки;
- добавлен модульный тест на фактическую структуру `courierInfo`;
- версия `0.3.2` развёрнута и проверена на реальном API: получено 1280 заказов, у 756 определён курьер, ошибок нет;
- публичная страница входа и stylesheet возвращают HTTP 200 через HTTPS.

## Подключение основного домена

Статус: завершено 15 сентября 2026 года.

- DNS `airobot.sushi03.ru` указывает на сервер `5.129.249.100`;
- домен добавлен в разрешённые хосты приложения и конфигурацию Nginx;
- действующий TLS-сертификат расширен на основной домен и настроен на автоматическое продление;
- технический адрес `5-129-249-100.sslip.io` временно сохраняется как резервный;
- `https://airobot.sushi03.ru/login` возвращает HTTP 200, HTTP перенаправляется на HTTPS;
- контейнеры приложения и PostgreSQL находятся в состоянии `healthy`.

## Этап 4 — Mango API Realtime и безопасный тестовый контур

Статус: выполняется 15 сентября 2026 года.

Подготовлено в версии `0.4.0`:

- публичный endpoint `POST /mango/events/call`;
- проверка `vpbx_api_key` и подписи `SHA-256` по исходной JSON-строке;
- отказ в приёме событий до настройки API Key и Salt;
- проверка формата и ограничение размера события;
- идемпотентное сохранение событий по `call_id` и `seq`;
- шифрование номеров участников звонка в базе;
- раздел «Звонки» для последних событий Mango;
- настройка единственного разрешённого тестового номера;
- документ безопасного сценария Starter → тестовый номер → локальная симуляция подтверждения;
- модульные тесты подписи и разбора события.

Развёрнуто и проверено:

- версия `0.4.0` работает на сервере, контейнеры `healthy`;
- `GET https://airobot.sushi03.ru/mango/events/call` сообщает `awaiting_credentials`;
- неподписанный POST до настройки ключей отклоняется с HTTP 503;
- таблица `mango_call_events` создана в PostgreSQL.

Для продолжения требуются `vpbx_api_key` и `vpbx_api_salt`, после чего можно выполнить реальную проверку API-коннектора Mango.
# 2026-09-15 — Настройки OpenAI и тестового контура

- В раздел `/settings` добавлены модель и голос OpenAI Realtime.
- Системный промпт вынесен в отдельное многострочное поле.
- Тестовый номер объединён с настройками ИИ-робота и проверяется в международном формате.
- API-ключ и тестовый номер продолжают храниться зашифрованно и не отображаются после сохранения.
- Версия приложения: `0.4.1`.

# 2026-09-17 — Исправление страницы настроек

- Устранён конфликт имени `items` с методом словаря в Jinja, вызывавший HTTP 500 после авторизации.
- Добавлен тест полного рендеринга формы настроек с параметрами OpenAI и тестового номера.
- Версия приложения: `0.4.2`.

# 2026-09-17 — SIP-подключение Mango

- Добавлен отдельный контейнер Asterisk с PJSIP и AMI.
- Реализована генерация приватной конфигурации из зашифрованных настроек админки.
- Добавлены применение конфигурации, проверка регистрации и отображение статуса в админке.
- Настроены G.711A, RFC2833, регистрация 180 секунд и ограниченный диапазон RTP 10000–10100 UDP.
- До реализации голосового робота все входящие вызовы отклоняются отдельным dialplan.
- Версия приложения: `0.5.0`.

# 2026-09-17 — OpenAI Realtime и ручной тестовый звонок

- Добавлен двусторонний TCP AudioSocket-мост Asterisk ↔ приложение.
- PCM16 8 кГц из телефонии преобразуется в PCM16 24 кГц для OpenAI и обратно.
- OpenAI Realtime работает через серверный WebSocket; ключ не передаётся браузеру.
- Включён server VAD с автоматическим созданием ответа и остановкой локального воспроизведения при реплике абонента.
- На странице «Звонки» добавлена кнопка ручного запуска и журнал последних тестов.
- Звонок разрешён только на единственный номер, заранее сохранённый в настройках; произвольный номер из формы не принимается.
- Одновременно разрешён только один тестовый звонок, длительность ограничена пятью минутами.
- iiko остаётся в режиме только чтения, результат разговора не меняет заказ.
- До устранения Mango `403 Forbidden` реальный дозвон заблокирован проверкой регистрации SIP.
- Версия приложения: `0.6.0`.

# 2026-09-17 — Углублённая диагностика SIP-регистрации Mango

- Перехвачен полный обмен при чистом запуске Asterisk: обычный `REGISTER` с `Expires: 180` получает штатный Digest challenge `401`, после авторизованного повторного запроса Mango возвращает `403 Forbidden` с внутренним текстом `77ee12`.
- Подтверждено, что DNS, UDP-порт `60000`, обратная доставка SIP-пакетов, NAT и доступность регистратора работают; `OPTIONS` получает `200 OK - keepalive`.
- Проверено отсутствие начальных/конечных пробелов и переноса строки в сохранённом SIP-пароле без раскрытия значения.
- A/B-тест `Contact: 225` вместо полного идентификатора не изменил результат.
- A/B-тест адреса регистрации `225` при сохранении полного Digest-логина не изменил результат.
- A/B-тест Digest-логина `225` не изменил результат.
- Порт `5060/UDP` не дал успешной регистрации; рабочий для обмена с данным узлом порт — `60000/UDP`.
- После тестов исходная конфигурация восстановлена, временная копия с секретом удалена.
- Основная оставшаяся гипотеза: переданное значение `400338531/225` может быть логином сотрудника для Личного кабинета/Контакт-центра, а не SIP-ID отдельной SIP-учётной записи. Для продолжения требуется полный SIP-адрес и пароль непосредственно из карточки SIP-учётной записи Mango либо подтверждение поддержки по серверному журналу и коду `77ee12`.

# 2026-09-22 — Mango API Realtime и SIP зарегистрированы

- В кабинете Mango включён API-коннектор и настроен базовый адрес внешней системы `https://airobot.sushi03.ru/mango`.
- `vpbx_api_key` и `vpbx_api_salt` сохранены в зашифрованных настройках приложения; проверка подписи webhook активна.
- Контрольный входящий звонок подтвердил доставку цепочки `events/call`: запросы прошли проверку подписи, получили HTTP 200 и сохранены в базе.
- Найдена фактическая SIP-учётная запись сотрудника с внутренним номером 225: SIP-ID `user9` в домене `vpbx400338531.mangosip.ru`.
- Подтверждено, что ранее переданное значение `400338531/225` являлось не SIP-ID; именно его использование вызывало `403 Forbidden` с внутренним кодом `77ee12`.
- Сохранённые настройки применены к Asterisk. Регистрация `sip:user9@vpbx400338531.mangosip.ru` на `60000/UDP` получила статус `Registered`.
- Для запуска безопасного тестового звонка остаётся сохранить OpenAI API Key и единственный разрешённый тестовый номер.

# 2026-09-22 — Исправление идентификатора исходящего SIP-вызова

- Первый ручной тест был передан Asterisk, но завершился тайм-аутом без соединения и без соответствующего исходящего события в Mango API Realtime.
- Установлено, что PJSIP подставлял во `From` внутренний номер сотрудника `225`, хотя выданный Mango SIP-ID — `user9`.
- Генератор PJSIP-конфигурации исправлен: для исходящего SIP-заголовка используется SIP-ID учётной записи, совпадающий с идентификатором регистрации и Digest-аутентификации.
- Обновлён модульный тест конфигурации; тесты SIP, Mango и iiko проходят. Локальный тест Realtime требует отсутствующую в системном Python зависимость `websockets` и будет повторно выполнен внутри контейнера.

# 2026-09-22 — Нормализация номера для исходящего SIP-вызова

- По API Realtime установлена точная причина неуспешной попытки: Mango завершила вызов менее чем за секунду с кодом `1131` («вызываемый номер недоступен»), до открытия аудиоканала.
- Хранимый тестовый номер по-прежнему проверяется и хранится в международном формате `+7…`.
- A/B-проверка показала, что для этой SIP-учётной записи Mango российский номер назначения должен передаваться как `8XXXXXXXXXX`: вызов получил `200 OK`, был отвечен и перешёл в состояние `Connected`.
- Для российских номеров SIP URI теперь формируется в формате `8…`; исходящий Caller ID остаётся в формате `7…` без плюса.
- Добавлен модульный тест номера назначения и Caller ID.

# 2026-09-22 — Диагностика OpenAI Realtime

- После успешного ответа на контрольный звонок Asterisk открыл AudioSocket и приложение попыталось подключиться к `gpt-realtime-2`.
- OpenAI отклонил как Realtime WebSocket, так и контрольные REST-запросы с HTTP 403 и кодом `unsupported_country_region_territory`.
- Ключ присутствует и передаётся сервером корректно; ошибка связана с регионом исходящего IP сервера, а не с SIP, моделью, промптом или форматом аудио.
- Для продолжения голосовой части нужен сервер/голосовой шлюз в поддерживаемом OpenAI регионе либо другой провайдер модели, официально доступный из текущего региона.
