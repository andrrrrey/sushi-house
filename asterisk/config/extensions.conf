[general]
static=yes
writeprotect=yes
clearglobalvars=no

[mango-inbound-locked]
exten => _X!,1,NoOp(Rejecting inbound Mango call while test mode is locked)
 same => n,Hangup(21)
exten => s,1,NoOp(Rejecting inbound Mango call while test mode is locked)
 same => n,Hangup(21)
